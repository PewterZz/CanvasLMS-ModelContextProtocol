import axios from 'axios';

// Create a default Axios instance
// You can add base URLs, default headers, interceptors, etc. here if needed later
const axiosInstance = axios.create();

export default axiosInstance; 